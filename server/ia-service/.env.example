# server/ia-service/.env.example
# Copie para .env e preencha os valores reais antes de rodar.
#
# Ollama local (rodando como systemd service no mesmo host)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=gemma4:e2b-it-qat

# Supabase
SUPABASE_URL=https://ozxpdccutroxtjqcpjea.supabase.co
SUPABASE_SERVICE_ROLE_KEY=cole-aqui-a-service-role-key-do-supabase

# Porta do serviço (NÃO expor publicamente; nginx faz proxy)
PORT=8000